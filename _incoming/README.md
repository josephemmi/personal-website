Drop zone for the canonical style guide file (e.g. `josephemmistyleguide.html`)
referenced in the site's CSS comments. Once it's here, the refactor will use
it as the single source of truth for every component's styling.
